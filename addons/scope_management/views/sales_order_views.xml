<odoo>
    <record id="view_order_form_inherit_scope_proposal" model="ir.ui.view">
        <field name="name">sale.order.form.inherit.scope.proposal</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- Ubicar pestaña Quote Builder -->
            <xpath expr="//page[@name='pdf_quote_builder']" position="inside">
                <div class="mb-4">
                    <div class="mb-2">
                        <h3>
                            Propuesta Tecnica de Alcances &gt;
                            <field name="related_scope_products" readonly="1"/>
                        </h3>
                        <button name="action_print_technical_proposal" type="object" string="Alcances" class="btn btn-secondary m-1"/>
                    </div>
                </div>
            </xpath>

        </field>
    </record>
</odoo>